# x-cli-demo

Static landing page for the x-cli project, ported from a Claude Design prototype.

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues (`xpzouying/x-cli-demo`) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical labels, each mapped to its own name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
